version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    # Wait out fresh releases before adopting them (supply-chain cooldown; zizmor dependabot-cooldown).
    cooldown:
      default-days: 7
    groups:
      actions:
        patterns:
          - "*"
