# Ukraine Alarm Pro

[![HACS Custom](https://img.shields.io/badge/HACS-Custom-orange.svg?style=for-the-badge)](https://github.com/custom-components/hacs)
![Version](https://img.shields.io/badge/version-0.3.0-blue?style=for-the-badge)
![Home Assistant](https://img.shields.io/badge/Home%20Assistant-2025.1%2B-41BDF5?style=for-the-badge&logo=home-assistant)

[![Quality Gate](https://sonarcloud.io/api/project_badges/measure?project=ABovsh_ukraine-alarm-pro&metric=alert_status)](https://sonarcloud.io/summary/new_code?id=ABovsh_ukraine-alarm-pro)
[![Reliability](https://sonarcloud.io/api/project_badges/measure?project=ABovsh_ukraine-alarm-pro&metric=reliability_rating)](https://sonarcloud.io/component_measures?id=ABovsh_ukraine-alarm-pro&metric=reliability_rating)
[![Security](https://sonarcloud.io/api/project_badges/measure?project=ABovsh_ukraine-alarm-pro&metric=security_rating)](https://sonarcloud.io/component_measures?id=ABovsh_ukraine-alarm-pro&metric=security_rating)
[![Maintainability](https://sonarcloud.io/api/project_badges/measure?project=ABovsh_ukraine-alarm-pro&metric=sqale_rating)](https://sonarcloud.io/component_measures?id=ABovsh_ukraine-alarm-pro&metric=sqale_rating)
[![Coverage](https://img.shields.io/sonar/coverage/ABovsh_ukraine-alarm-pro?server=https%3A%2F%2Fsonarcloud.io&style=for-the-badge&logo=sonarcloud&label=coverage)](https://sonarcloud.io/component_measures?id=ABovsh_ukraine-alarm-pro&metric=coverage)

[English](README.md) · **Українська**

Інтеграція повітряних тривог для Home Assistant з **push-оновленнями** (затримка ~1 с) через
анонімний WebSocket, на якому працює офіційна [мапа тривог](https://map.ukrainealarm.com/) —
**без API-ключа** — з автоматичним переходом на опитування siren.pp.ua, якщо WebSocket
недоступний.

## Чим це краще за core-інтеграцію `ukraine_alarm`

Вбудована інтеграція опитує волонтерський проксі siren.pp.ua кожні 10 с **окремо для кожного
регіону** з таймаутом 10 с, і під навантаженням проксі це періодично збоїть. Ця інтеграція:

- **Отримує push** через Centrifugo-WebSocket офіційної мапи (без ключа, затримка ~1 с)
- Одне з'єднання обслуговує **всі** обрані регіони (у core — окремий цикл опитування на
  кожен регіон)
- **Бачить тривоги, оголошені нижче за ваш регіон.** Більшість тривог оголошують для
  окремого району чи громади, а не для цілої області. Якщо звіряти лише точний id регіону,
  сутність області мовчить, поки її райони під повітряною тривогою — на момент написання це
  стосувалося 6 областей із 29 одночасно. Тут кожен регіон успадковує тривоги і від
  **предків**, і від **нащадків**, тому жоден рівень не стає сліпою зоною
- **Показує тип загрози**, а не лише «є/немає»: повітряна тривога, артобстріл, вуличні бої,
  хімічна чи ядерна загроза — з переліком, який саме регіон оголосив кожну тривогу
- **Заповнює стан за кілька секунд після перезапуску** — карта тривог зчитується одразу, а не
  чекає, поки джерело щось опублікує
- Автоматично переходить на опитування проксі (раз на 60 с, таймаут 30 с), самостійно
  повертається на WebSocket і повідомляє про роботу в резервному режимі в розділі
  **Виправлення**
- **Ніколи не дає хибного «відбою».** Сутності зберігають останній відомий стан під час
  втрати зв'язку, незрозуміла відповідь джерела вважається помилкою, а не тишею, а
  застарілість даних показана явно (`binary_sensor.uap_data_stale`), а не прихована
- Має **українську та англійську** локалізацію, діагностику й лічильник тривог по країні

## Як регіон стає «у тривозі»

Тривоги публікуються на тому адміністративному рівні, на якому їх оголосили — іноді це
область, частіше окремий район чи громада. Тому обраний регіон вважається у тривозі, коли її
має **він сам**, будь-який **предок** або будь-який **нащадок**:

| Ви обрали | Тривогу оголошено для | Результат |
| --- | --- | --- |
| Сумська громада | Сумська область (тривога по всій області) | у тривозі (предок) |
| Сумська область | Сумський район | у тривозі (нащадок) |
| Сумський район | Сумська громада | у тривозі (нащадок) |

Обирайте будь-який зручний рівень — сутність області не пропустить тривогу рівня району.

## Сутності

| Сутність | Тип | Примітки |
| --- | --- | --- |
| `sensor.uap_<id>_threat` | enum | `none` / `unrecognized` / `air` / `artillery` / `urban_fights` / `chemical` / `nuclear` — найвища активна загроза; атрибут `active_alerts` перелічує кожну тривогу разом із регіоном-джерелом (максимум 25 записів; справжню кількість містить `active_alert_count`) |
| `binary_sensor.uap_<id>_alert` | safety | увімкнений, поки активна будь-яка загроза |
| `sensor.uap_transport` | діагностична | `websocket` або `polling` |
| `sensor.uap_last_update` | діагностична | час останнього отриманого знімка |
| `sensor.uap_active_regions` | діагностична | кількість регіонів з активною тривогою по всій країні |
| `binary_sensor.uap_data_stale` | діагностична, problem | увімкнений, якщо знімків не було 15 хвилин |

Два зауваження щодо самих даних:

- `sensor.uap_active_regions` ніколи не дорівнює 0 — окуповані території (Луганська область,
  Автономна Республіка Крим) у джерелі мають постійно активні тривоги. Атрибут `region_ids`
  показує, які саме регіони враховано.
- Невідомий інтеграції тип тривоги показується як `unrecognized` і один раз потрапляє в лог
  із попередженням, щоб його додали в наступному релізі.

## Надійність

- **Стан одразу після перезапуску.** WebSocket не віддає історію, тому перший знімок
  береться з резервного джерела опитування, замість чекати, поки карта десь по країні
  зміниться — а це може тривати кілька хвилин.
- **Сторожовий таймер.** Якщо 15 хвилин не було жодного push, карта тривог звіряється з
  резервним джерелом. Збігається — країна просто спокійна, з'єднання не чіпаємо; не
  збігається або джерело недоступне — сокет закривається, і супервізор проходить свій
  звичайний шлях перепідключення та деградації.
- **Деградація і відновлення.** Три поспіль **короткі** збої WebSocket переводять інтеграцію
  на опитування й створюють повідомлення у «Виправленнях»; довга справна сесія, яку сервер
  завершив сам, до цього ліміту не зараховується. Інтеграція продовжує пробувати WebSocket і
  прибирає повідомлення після відновлення.
- **Непридатні дані — це помилка, а не тиша.** Відповідь джерела, яку інтеграція не може
  розібрати, вважається помилкою транспорту, а не «тривог ніде немає».
- **Без хибного «відбою».** Сутності зберігають останній відомий стан, поки транспорт
  недоступний; `binary_sensor.uap_data_stale` показує, коли цьому стану вже не можна довіряти.

Рекомендована умова для автоматизацій:

```yaml
condition:
  - condition: state
    entity_id: binary_sensor.uap_data_stale
    state: "off"
```

## Встановлення

HACS → користувацький репозиторій → `ABovsh/ukraine-alarm-pro` → встановити → додати
інтеграцію → обрати регіони (доступний повний список аж до рівня громад).

Щоб змінити перелік регіонів пізніше: інтеграція → **Налаштувати**. Після збереження
сутності створюються заново; сутності вилучених регіонів потрібно видалити вручну з реєстру
сутностей.

## Усунення несправностей

Завантажте **Налаштування → Пристрої та служби → Ukraine Alarm Pro → Діагностика** — там є
режим транспорту, вік останнього знімка, налаштовані регіони та поточні активні тривоги.
Приховувати в них нічого не потрібно: інтеграція повністю анонімна.

## Джерела даних

Дані про тривоги надходять із [ukrainealarm.com](https://map.ukrainealarm.com/) (push) та
волонтерського проксі [siren.pp.ua](https://siren.pp.ua/) (резервне опитування).
